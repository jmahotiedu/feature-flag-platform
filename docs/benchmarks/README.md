# Benchmarks

Use the included load scripts to generate reproducible reports.

## Control-plane

```bash
npm run bench
```

## SDK evaluation latency

```bash
npm run bench:sdk
```

Reports are emitted into this directory as JSON.
