{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {
      "type": "timeseries",
      "title": "Request Rate",
      "targets": [{"expr": "sum(rate(ff_http_requests_total[1m]))", "refId": "A"}],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "P95 Latency (ms)",
      "targets": [{"expr": "histogram_quantile(0.95, sum(rate(ff_http_request_duration_ms_bucket[5m])) by (le))", "refId": "A"}],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "Error Rate",
      "targets": [{"expr": "sum(rate(ff_http_requests_total{status=~\"5..\"}[5m])) / sum(rate(ff_http_requests_total[5m]))", "refId": "A"}],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8}
    },
    {
      "type": "timeseries",
      "title": "Publish Propagation Delay",
      "targets": [{"expr": "avg(ff_publish_propagation_delay_ms)", "refId": "A"}],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8}
    }
  ],
  "schemaVersion": 39,
  "title": "Feature Flag SLO Dashboard",
  "version": 1
}
